The working source for this development turn is the project root.
When V0.1.1 passes real acceptance tests, copy/freeze the full source snapshot here or create the
release archive under `06_release/`.
