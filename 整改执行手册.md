# 材数智能体回答链路整改执行手册

> 本手册供执行智能体直接使用。所有路径、代码位置、验收标准均已明确。执行顺序：P0-1 先做，P0-2 为主路径改造，P0-3 依赖 P0-2，P0-4/P0-5/P0-6 可与 P0-2 并行。

---

## 0. 环境与路径

| 项 | 值 |
|---|---|
| 本项目根目录 | `E:\Trae\Demo3\materials-research-agent` |
| D盘参考项目根目录 | `D:\HuaweiMoveData\Users\linyu\Documents\ChatGPT\materials-research-agent` |
| 开发手册 | `E:\project\materials-research-agent\材数智能体_学生实施与代码生成手册_V1.0 (1).md` |
| 后端语言 | Python（FastAPI） |
| 前端 | React（`frontend/` 目录，Vite） |
| 数据库 | MySQL（业务库 `materials`，只读） |

**执行前必读文件**（按顺序）：
1. 本手册第 1-4 节（上下文背景、链路图、字段速查）
2. 每个 P0 项的"前置阅读文件"

---

## 1. 上下文背景

### 1.1 三个实测问题（验收用例）

#### 实测问题 1：相似配方检索
- **用户输入**：`查找与 EXP-128 相似的配方，并结合历史案例`
- **当前回答的问题**：
  - 只返回相似度排名和样品 ID（EXP-330、EXP-302 等），**没有配方原料用量、性能指标、实验结果**
  - 错误判定"项目 ID -1360 的名称存在冲突"——实际 EXP-xxx 是**样品名称**，不是项目名称
  - "数据冲突保留"是内部技术细节，用户不需要
- **预期输出**：TopN 相似样品，每个含配方（原料+用量）、关键性能（密度差、持液量）、实验状态、推荐等级

#### 实测问题 2：工艺最像的样品
- **用户输入**：`找与 EXP-128 工艺最像的样品，并结合历史资料`
- **当前回答的问题**：
  - 直接返回"无法找到与 EXP-128 工艺最相似的样品"
  - 原因是"参照样品在所选范围内没有可用于计算的唯一数值字段"（`insufficient_reference_fields`）
  - **没有降级方案**，没有告诉用户缺什么字段、怎么补
- **预期输出**：工艺字段缺失时，降级为配方相似结果 + 明确提示"EXP-128 的 craft_param 字段缺少可计算数值，建议补充后重查"

#### 实测问题 3：按目标性能反查
- **用户输入**：`查找密度差大于 100 且持液量小于 0.1 的历史样品，并结合历史资料`
- **当前回答的问题**：
  - 返回"满足条件的样品共 50 个"，但只罗列前 10 个**样品 ID**，没有配方、工艺、性能数值
  - **没有排序、没有推荐**
  - 强行关联 ABS 材料通用知识（微观结构、缺陷分析），与"萃取配方、密度差、持液量"业务场景无关
  - "共有 153 条相关记录被系统省略"——不说明省略规则、是否影响结论
- **预期输出**：TopN 合格样品卡片，含配方/工艺/性能数值，按稳定性/性能余量排序，给出首选推荐；历史资料仅在与当前样品实体关联时引用

### 1.2 20 场景清单

| 编号 | 场景名称 | 类型 | workflow |
|---|---|---|---|
| 1 | 相似配方检索 | 查询 | hybrid_search_rank |
| 2 | 相似样品/相似实验检索 | 查询 | hybrid_search_rank |
| 3 | 按目标性能反查历史方案 | 查询 | hybrid_search_rank |
| 4 | 配方-工艺-性能联查 | 分析 | evidence_profile |
| 5 | 原料使用效果查询 | 查询 | hybrid_search_rank |
| 6 | 原料替代历史检索 | 查询 | hybrid_search_rank |
| 7 | 失败配方/失败实验检索 | 查询 | hybrid_search_rank |
| 8 | 关键变量识别 | 分析 | historical_analysis |
| 9 | 配方/工艺窗口发现 | 分析 | process_window |
| 10 | 多性能冲突分析 | 分析 | historical_analysis |
| 11 | 批次差异分析 | 分析 | historical_analysis |
| 12 | 异常与失效案例检索 | 查询 | hybrid_search_rank |
| 13 | 竞品对标 | 查询 | hybrid_search_rank |
| 14 | 新项目冷启动 | 方案 | research_cold_start |
| 15 | 测试结果自动关联 | 查询 | result_feature_ingestion |
| 16 | 图谱/曲线结果复用 | 查询 | result_feature_ingestion |
| 17 | 项目知识快速问答 | 分析 | evidence_profile |
| 18 | 自动生成阶段总结 | 方案 | stage_report |
| 19 | 模型版本与实验回流 | 方案 | experiment_loopback（接口预留） |
| 20 | 跨项目复用 | 查询 | cross_project_query |

**场景类型定义**：
- **查询类**（1,2,3,5,6,7,12,13,15,16,20）：用户要的是数据列表，回答正文 1-2 句总结 + 数据卡片
- **分析类**（4,8,9,10,11,17）：用户要的是结论+推理，回答正文 2-4 句结论 + 证据支撑
- **方案类**（14,18,19）：用户要的是可执行方案，结构化模板输出

### 1.3 D盘参考项目可借鉴文件

| 本项目要做的事 | D盘参考文件 | 借鉴点 |
|---|---|---|
| P0-2 聚合器主路径+结构化上下文 | `D:\HuaweiMoveData\Users\linyu\Documents\ChatGPT\materials-research-agent\agent\core.py` | `_full_profile` 把配方/工艺/性能组装为结构化对象注入 LLM 上下文 |
| P0-3 数据卡片渲染 | `D:\HuaweiMoveData\Users\linyu\Documents\ChatGPT\materials-research-agent\web-react\src\features\chat\evidence\FactView.tsx` | SampleCard（KV表）、CompareView（diff表）、MatchesView（列表）渲染模式 |
| P0-4 相似度降级 | `D:\HuaweiMoveData\Users\linyu\Documents\ChatGPT\materials-research-agent\agent\core.py` | `_metric_missing_response`：目标样品无指标时直接说"没有数据"+探查相邻样品 |
| P0-5 回答后处理 | `D:\HuaweiMoveData\Users\linyu\Documents\ChatGPT\materials-research-agent\agent\core.py` | `_sanitize_structured_answer`（剔JSON/内部ID/计划前缀）、`_guardrails`（自动注入因果/单位/对照假设）、`_validate_citations`（结论必须引用真实record_id） |
| P0-1 Markdown渲染 | `D:\HuaweiMoveData\Users\linyu\Documents\ChatGPT\materials-research-agent\web-react\src\features\chat\AnswerMarkdown.tsx` | D盘用 `react-markdown` 库，无此 bug；本项目自研 MarkdownView，需自行修复 |

---

## 2. 系统链路图

### 2.1 后端调用链

```
POST /agent-api/api/v1/chat-ui/stream (SSE)
  └── api/chat_ui.py
       └── orchestration/chat_ui_graph.py → invoke_chat_ui_graph()
            └── agent/deepseek_intent_router.py（意图识别 → scenario_id）
                 └── skills/hybrid_research_qa.py → HybridResearchQASkill.answer()
                      ├── _resolve_scope()（权限过滤）
                      ├── _structured_strategy()（选择检索策略）
                      │    ├── material_intelligence.execute_intent("similar_samples", ...)
                      │    ├── material_intelligence.execute_intent("find_samples_multi_condition", ...)
                      │    └── registry.execute("get_sample_context" / "list_samples_for_analysis")
                      ├── 并行：MySQL 检索 + 向量检索
                      ├── EvidenceFrameBuilder.build()（证据框架）
                      ├── _synthesize()（LLM 综合 → answer 文本）
                      └── 返回 {answer, evidence_frame, synthesis, warnings, ...}
```

### 2.2 前端渲染链

```
Message 组件 (App.jsx L763)
  └── div.bubble
       ├── AnalysisProgress（前置记录，SSE 事件渲染）
       ├── EngineTaskCard（引擎任务，异步轮询）
       ├── div.content → MarkdownView（回答正文渲染）← P0-1 改这里
       ├── EvidenceFrameCard（证据框架卡片）← P0-3 数据卡片加在这里
       ├── ResearchScenarioCard（场景卡片）
       ├── EngineWorkflowCard / ModelingCards / OptimizationCards / ...
       ├── CopyControl（复制答案按钮）
       └── Detail（执行详情，折叠）
```

### 2.3 每个 P0 项的改动定位

| P0项 | 后端改动 | 前端改动 |
|---|---|---|
| P0-1 | 无 | `frontend/src/MarkdownView.jsx` |
| P0-2 | `agent/scenario_aggregator.py` + `skills/hybrid_research_qa.py` + `agent/evidence_context.py` | 无 |
| P0-3 | `skills/hybrid_research_qa.py`（把聚合摘要转 data_cards） | 新增 `DataCards.jsx` + `App.jsx` |
| P0-4 | `skills/material_intelligence.py`（`_similar_samples`） | 无 |
| P0-5 | `skills/hybrid_research_qa.py`（`_sanitize_answer`） + `agent/evidence_frame.py`（实体对齐） | 无 |
| P0-6 | `skills/hybrid_research_qa.py`（`_vector_query`） + `agent/vector_search.py` | 无 |

---

## 3. 数据库字段速查

### 3.1 `eln_sample` 表核心 JSON 字段

| 字段 | 类型 | 格式 | 说明 |
|---|---|---|---|
| `recipes` | JSON | `{"R3-401": 0.00175, "R3-402": "..."}` | 配方，key 为 `R3-{原料ID}`，value 为用量 |
| `craft_param` | JSON | `{"S101": 30, "S102": 20, ...}` | 工艺参数，key 为 `S{字段ID}`，value 为参数值（可能为 null/字符串/对象） |
| `performances` | JSON | `{"P201": 112, "P202": 0.082, ...}` | 性能，key 为 `P{字段ID}`，value 为性能值 |
| `service_performances` | JSON | 同上 | 服役性能 |
| `conditions` | JSON | `{"测试温度": 25, ...}` | 测试条件 |
| `craft_detail` | JSON | 自由结构 | 工艺详情 |

### 3.2 字段 ID 解析规则

- `R3-{id}` → 查 `sample_materials` 表获取原料名称、单位
- `S{id}` / `P{id}` → 查 `data_column` 表获取字段名称、单位、所属 section（process/performance）
- 解析代码在 `agent/tools.py` 的 `_resolve_prefetched_fields()`（L425-447）和 `list_samples_for_analysis()`（L268-410）
- `get_sample_context()`（L134-213）用 `resolver.resolve_formula()` / `resolver.resolve_dynamic()` 解析

### 3.3 关键表

| 表名 | 作用 |
|---|---|
| `eln_sample` | 样品主表（含 JSON 配方/工艺/性能） |
| `sample_materials` | 原料/配方字段定义（R3-id 对应名称） |
| `data_column` | 工艺、性能等动态字段定义（S-id/P-id 对应名称） |
| `mat_project` | 项目主表 |
| `eln_synthesis_exp` | 合成实验记录 |
| `eln_verify_item` | 测试/验证项目 |
| `eln_verify_exp` | 验证实验记录 |
| `archive_data` | 数据归档表 |

---

## 4. P0-1：MarkdownView 渲染修复

**优先级**：最高（回答都看不见，后续无法验证）
**改动文件**：`frontend/src/MarkdownView.jsx`
**前置阅读**：`frontend/src/MarkdownView.jsx` 全文（181行）、`frontend/src/styles.css` 中 `.content` 和 `.markdownView` 样式

### 4.1 当前代码问题定位

| 行号 | 问题 | 后果 |
|---|---|---|
| L144-153 | block 仅按空行分割 | 标题后无空行时，标题+正文在一个 block |
| L94-97 | heading 分支只取 `first` 行渲染，`lines.slice(1)` 被丢弃 | **标题后正文丢失**（实测问题1/2/3的回答只显示标题） |
| L135 | 默认分支 `lines.join("\n")` 塞进一个 `<p>` | **多行堆积成一段** |
| L99 | 列表判定 `lines.every(...)` 全匹配才渲染 | 列表中混入非列表行时整个降级为 `<p>` |
| L113 | 有序列表同上 | 同上 |
| L127 | 引用块同上 | 同上 |
| L145 | 代码块 `inCode = !inCode`，未闭合时不保护 | 后续所有内容被误渲染为代码 |

### 4.2 改动方案

#### 改动 1：重写 block 分割逻辑（L138-160 `MarkdownView` 组件）

将"按空行分割"改为"按行类型分割"：

```
遍历每一行：
- 如果是代码块开始/结束 → 代码块独立（保持现有 inCode 逻辑）
- 如果是标题行（/^#{1,6}\s+/）→ 标题独立成 block，不与后续行合并
- 如果是表格分隔行（isTableSeparator）→ 与上一行+后续表格行合并为表格 block
- 如果是列表行（/^\s*[-*+]\s+/ 或 /^\s*\d+[.)]\s+/）→ 连续列表行合并为一个 block
- 如果是引用行（/^>/）→ 连续引用行合并为一个 block
- 如果是空行 → 结束当前 block
- 其他 → 文本行，连续文本行合并为一个 block（但遇到标题/列表/表格时截断）
```

**关键**：标题行必须独立成 block，确保 `### 结论` 后紧跟的正文不会被标题分支吃掉。

#### 改动 2：heading 分支处理剩余行（L94-97）

当前：
```jsx
if (heading) {
    return <Heading level={heading[1].length}>{renderInline(heading[2], `h-${index}`)}</Heading>;
}
```

改为：标题渲染后，将 `lines.slice(1)` 递归渲染。用 `React.Fragment` 包裹：
```jsx
if (heading) {
    const rest = lines.slice(1);
    return (
        <>
            <Heading level={heading[1].length}>{renderInline(heading[2], `h-${index}`)}</Heading>
            {rest.length > 0 && rest.map((line, i) => (
                <p key={`h-rest-${index}-${i}`}>{renderInline(line, `h-rest-${index}-${i}`)}</p>
            ))}
        </>
    );
}
```
（如果改动1已确保标题独立成 block，则 `lines.slice(1)` 为空，此改动为双保险；但建议两个改动都做）

#### 改动 3：默认段落分支拆分多行（L135）

当前：
```jsx
return <p>{renderInline(lines.join("\n"), `p-${index}`)}</p>;
```

改为：多行时拆成多个 `<p>`：
```jsx
if (lines.length === 1) {
    return <p>{renderInline(lines[0], `p-${index}`)}</p>;
}
return (
    <>
        {lines.map((line, i) => (
            <p key={`p-${index}-${i}`}>{renderInline(line, `p-${index}-${i}`)}</p>
        ))}
    </>
);
```

#### 改动 4：列表/引用/表格判定放宽（L99/L113/L127/L69）

将 `lines.every(...)` 改为"连续匹配行渲染，遇到不匹配行截断"。具体做法：在 block 分割阶段（改动1）已经确保列表 block 内全是列表行，所以这里 `lines.every` 自然成立。**如果改动1做完整，改动4不需要额外改代码**。

#### 改动 5：代码块闭合保护（L145）

在 `MarkdownView` 组件遍历结束后，如果 `inCode === true`，自动补闭合，并在控制台输出 warning：
```jsx
if (inCode && current.length) {
    blocks.push(current.join("\n"));
    console.warn("MarkdownView: unclosed code block, auto-closed");
}
```

### 4.3 验收标准

1. 输入 `### 结论\n正文内容\n### 证据依据\n- 列表项` → 标题、正文、列表均完整渲染，无内容丢失
2. 输入多段无空行文本 → 各段独立显示，有段落间距
3. 输入未闭合代码块 → 后续内容不被误渲染为 `<pre>`
4. 三个实测问题的回答完整可见
5. 列表、表格、引用块渲染正常

### 4.4 验证方法

- 前端启动后，在聊天框输入三个实测问题，观察回答渲染
- 或在浏览器控制台直接测试 `MarkdownView` 组件渲染

---

## 5. P0-2：聚合器主路径 + 压缩器降级

**优先级**：高（P0-3 的前置）
**改动文件**：
- `agent/scenario_aggregator.py`（已存在，扩展为唯一主路径）
- `skills/hybrid_research_qa.py`（`_synthesize` 输入改为聚合摘要）
- `agent/evidence_context.py`（`EvidenceContextCompressor` 降级为尺寸兜底）
**前置阅读**：`agent/scenario_aggregator.py` 全文、`skills/hybrid_research_qa.py`（`answer`/`_synthesize`）、`agent/evidence_context.py` 全文、`agent/evidence_frame.py` 全文

### 5.1 当前代码问题（已核验）

- `value/unit` 已在 `EvidenceFrameBuilder.add_mysql_result` 填充，实测 4916 条记录中 4166 条带值；旧 P0-2 "未填充" 判断错误
- 真实根因：`EvidenceFrameBuilder` 把 563 个样品展开成 4916 条扁平记录，其中 563 条纯 `record_reference` 先入队；压缩器对所有 mysql 记录打同分，Top-80 被 reference 编号占据，配方数值被挤掉
- `EvidenceContextCompressor` 当前承担两件事：内容选择（按分数挑 80 条）+ 尺寸兜底。前者应删除，只保留后者
- 已提交 `agent/scenario_aggregator.py`（0fcdb47），但目前是"旁挂"派生记录，压缩器仍在并行做扁平选择，形成两条路径

### 5.2 改动方案

#### 改动 1：聚合器成为 LLM 输入唯一主路径

在 `hybrid_research_qa.answer()` 中：
- 聚合摘要（`aggregate_scenario_result` 输出）作为 LLM 的 `COMPACT EVIDENCE CONTEXT` 主内容
- LLM prompt 中 `records` 数组只保留"引用锚点"：每个聚合行对应的少量 record_id（用于引用校验），不再放全部扁平记录

#### 改动 2：压缩器降级为尺寸兜底

在 `agent/evidence_context.py` 中：
- 删除 `_select_records` / `_record_score` 的"按分数挑选内容"逻辑
- `build()` 只接收"已聚合的结构化摘要"，职责变为：摘要超过 `max_chars` 时按字段优先级截断（先删 warnings、conflicts，再删低优先级 TopN 行），保留结论必需数值

#### 改动 3：EvidenceFrame 职责调整

`agent/evidence_frame.py` 不改数据填充（已正确），但明确职责：
- EvidenceFrame = 审计 + 引用校验 + 前端证据卡数据源
- EvidenceFrame 不进入 LLM 主上下文

#### 改动 4：同步修改 SYSTEM PROMPT（`_synthesize` L1092-1106）

当前 prompt 与聚合摘要存在三处冲突，必须同步修改：
- **删除**第2条"每个事实性结论后用 record_id 标注证据，例如 [evf-xxx/record-id]"——聚合摘要中没有 record_id，LLM 按此要求会编造 record_id（违反手册约束1）。改为"引用样品名称即可，完整数据由系统卡片展示"
- **更新**描述："已由后端完成...有界压缩"改为"已由后端完成结构化聚合"
- **查询类场景输出格式**：从强制"结论、证据依据、风险/缺口"三段式，改为"2-4句总结+推荐，完整数据由系统卡片展示"（分析类和方案类仍保留三段式）

### 5.3 验收标准

1. LLM 主上下文是结构化摘要（TopN 样品 + 配方/工艺/性能数值），不是扁平记录堆
2. 问"EXP-128 相似配方"，回答中出现具体数值（如"P507=0.00175""密度差=112"）
3. 压缩器只做尺寸兜底，不再挑选"哪条记录更重要"
4. EvidenceFrame 仍完整保留（审计/前端可用），与 LLM 输入解耦

### 5.4 验证方法

- 在 `_synthesize` 加临时日志打印 LLM 输入，确认是聚合摘要而非扁平记录
- 调用 `/chat-ui`，检查回答含具体数值
- 单元测试断言压缩器不再返回"按分数选择"的扁平记录列表

---

## 6. P0-3：确定性数据卡片层

**优先级**：高（依赖 P0-2）
**改动文件**：
- 后端：`skills/hybrid_research_qa.py`（`answer()` 返回值，把 `aggregated_summary` 转为 `data_cards`）
- 前端：新增 `frontend/src/DataCards.jsx`，修改 `frontend/src/App.jsx` Message 组件
**前置阅读**：`skills/hybrid_research_qa.py` L337-374（`answer()` 返回结构）、`frontend/src/App.jsx` L763-774（Message 组件）、`frontend/src/EvidenceFrameCard.jsx`、D盘 `FactView.tsx`

### 6.1 当前代码问题

- `answer()` 返回值中没有结构化数据卡片，只有 LLM 生成的 `answer` 文本和 `evidence_frame` 元数据
- 前端 `EvidenceFrameCard` 只展示 record 元数据（record_id/source_type/subject_id/attribute），**不展示数值**
- 结果：查询类场景用户看不到数据列表（实测问题1/3）

### 6.2 改动方案

#### 改动 1：`data_cards` 由聚合器输出派生

不新增第二套 `_build_data_cards`。`data_cards` 直接复用 `aggregate_scenario_result` 的 `aggregated_summary`：

- 在 `answer()` 返回前，把 `aggregated_summary` 转换为前端可渲染的 `data_cards` 结构
- LLM 输入和前端数据卡片共用同一份聚合摘要，避免两条路径结果不一致

`data_cards` 由 `aggregated_summary` 派生的规则：

```
根据 scenario_id 决定卡片类型：
- 场景 1,2（相似）→ card_type="sample_list"，取 similarity 结果的 Top5
- 场景 3,14（性能反查/冷启动）→ card_type="sample_list"，取多条件筛选结果的 Top5，按性能余量排序
- 场景 5,6（原料效果/替代）→ card_type="sample_list"，取匹配样品 Top5
- 场景 7,12,13（失败/异常/竞品）→ card_type="sample_list"
- 场景 4（联查）→ card_type="compare"，取目标样品的完整 profile
- 场景 8,9,10,11（分析）→ card_type="metric_table"，取分析结果
- 场景 15,16,17,18,19,20 → 暂不生成卡片（或生成简单列表）

每个 sample_list 卡片的 item 结构：
{
  "sample_id": int,
  "sample_name": str,
  "similarity": float (可选),
  "formula": [{"name": str, "value": any, "unit": str}],
  "process": [{"name": str, "value": any, "unit": str}],
  "performance": [{"name": str, "value": any, "unit": str}],
  "status": str (如"已放大3批""小试稳定""持液量接近上限"),
  "recommendation": str (如"首选复用""高性能备选""谨慎使用")
}

compare 卡片结构：
{
  "left": {sample_name, formula, process, performance},
  "right": {sample_name, formula, process, performance},
  "diff": [{"field": str, "left": any, "right": any, "changed": bool}]
}

metric_table 卡片结构：
{
  "title": str,
  "columns": [str],
  "rows": [[any]]
}
```

**推荐等级判定逻辑**（确定性，不依赖 LLM）：
- 首选复用：性能指标全部达标 + 有放大记录 + 性能余量>10%
- 高性能备选：性能指标全部达标 + 性能余量>20% 但无放大记录
- 谨慎使用：某性能指标接近上限（余量<5%）或有失败记录
- 不推荐：性能指标不达标

将 `data_cards` 加入 `answer()` 返回值。

#### 改动 2：前端新增 `DataCards.jsx` 组件

新建 `frontend/src/DataCards.jsx`，实现三种卡片渲染：

1. **SampleListCard**：样品列表卡片
   - 每个样品一个子卡片，标题为"样品名 + 相似度/推荐等级"
   - 配方/工艺/性能用 KV 行展示（名称: 值 单位）
   - 推荐等级用颜色标签（首选=绿色、备选=蓝色、谨慎=橙色、不推荐=红色）
   - 参考 D盘 `SampleCard` 的 KV 表样式

2. **CompareCard**：双样品对比卡片
   - 三列：字段名 | 样品A | 样品B
   - 有差异的行高亮
   - 参考 D盘 `CompareView` 的 diff 表样式

3. **MetricTableCard**：指标统计表
   - 普通表格渲染

#### 改动 3：前端 Message 组件渲染数据卡片

在 `frontend/src/App.jsx` 的 Message 组件（L771）中，在 `MarkdownView` 之后、`EvidenceFrameCard` 之前插入：

```jsx
{m.data?.data_cards?.length > 0 && <DataCards cards={m.data.data_cards} />}
```

#### 改动 4：回答正文与卡片的关系

- 查询类场景：LLM 正文只写 2-4 句总结+推荐（结果数量、推荐及理由、风险提示），完整数据在卡片里
- 在 `_synthesize` 的 SYSTEM PROMPT 中增加："查询类场景（相似配方、性能反查、原料效果等）正文只写2-4句总结和推荐，不要重复罗列数据，完整数据由系统卡片展示。"

### 6.3 验收标准

1. 问"EXP-128 相似配方"，回答下方出现 Top5 样品卡片，含配方/性能/推荐等级
2. 问"密度差>100且持液量<0.1的样品"，出现 Top5 合格样品卡片，按推荐等级排序
3. 卡片数据与后端 mysql_result 一致（可追溯）
4. 卡片样式正确，KV 行可读

### 6.4 验证方法

- 调用 `/chat-ui` 接口，检查返回的 `data.data_cards` 字段是否存在且结构正确
- 前端启动后观察卡片渲染

---

## 7. P0-4：相似度降级链

**优先级**：中（可与 P0-2 并行）
**改动文件**：`skills/material_intelligence.py`（`_similar_samples` 方法）
**前置阅读**：`skills/material_intelligence.py` L352-370（`_similar_samples` 调用处）、L94-141（`execute_intent` 方法）、`agent/tools.py` L134-213（`get_sample_context` 返回结构）

### 7.1 当前代码问题

- `_similar_samples` 只对齐**数值型**配方/工艺字段（L357"按字段名称和单位对齐数值型配方、工艺字段"）
- 当参照样品工艺字段（`craft_param`）为 null/字符串/对象时，`comparable_candidate_count=0`
- 直接返回失败，**没有降级方案**（实测问题2的根因）

### 7.2 改动方案

在 `skills/material_intelligence.py` 的 `_similar_samples` 方法中，当 `comparable_candidate_count == 0` 时，按以下顺序降级：

#### 一级降级：配方文本相似度

用配方原料名的 Jaccard 相似度计算（不要求数值）：
```
对参照样品的配方原料名集合 A，每个候选样品的原料名集合 B：
  similarity = |A ∩ B| / |A ∪ B|
按 similarity 降序取 Top5
返回时标注："基于原料组成匹配，工艺参数缺失，相似度仅供参考"
```

#### 二级降级：同项目样品列表

如果配方也为空（极端情况），返回同项目最近创建的 N 个样品：
```
筛选 project_id == 参照样品.project_id 的样品
按 create_time 降序取 Top5
标注："无足够参数计算相似度，以下为同项目历史样品"
```

#### 三级降级：明确字段缺失提示

如果以上都不可用，返回：
```python
{
    "status": "insufficient_fields",
    "analysis_type": "similar_samples",
    "ranking": [],
    "comparable_candidate_count": 0,
    "missing_fields": ["craft_param（工艺参数为空）"],
    "message": "EXP-128 的 craft_param 字段缺少可计算的数值型工艺参数（如温度、时间、搅拌速率等），无法计算工艺相似度。建议在 eln_sample.craft_param 中补充工艺参数后重新查询。当前可参考配方相似结果。",
    "warnings": ["工艺参数缺失，已降级为配方文本相似度"]
}
```

#### 相似度计算扩展（可选，中长期）

当前仅数值字段，可扩展为混合相似度：
- 数值字段：欧氏距离/余弦相似度
- 文本字段（原料名、工艺描述）：Jaccard/编辑距离
- 类别字段（项目、样品类型）：精确匹配加权
- 三类加权求和，任何一类有数据即可计算

**注意**：本次整改只做降级链（一级+二级+三级），混合相似度作为后续优化。

### 7.3 验收标准

1. 参照样品工艺字段为空时，不返回"无法找到"，而是返回配方相似结果 + 缺失提示
2. 用户能明确知道"缺什么字段、怎么补"
3. 工艺字段完整时，相似度计算结果与现有一致（无回归）
4. 实测问题2的回答包含降级结果和字段补全建议

### 7.4 验证方法

- 构造一个 craft_param 为空的样品，调用 similar_samples，检查返回是否降级
- 构造一个 craft_param 完整的样品，确认相似度计算正常

---

## 8. P0-5：回答后处理守卫

**优先级**：中（可与 P0-2 并行）
**改动文件**：`skills/hybrid_research_qa.py`（新增 `_sanitize_answer` 方法，在 `_synthesize` 返回后调用）
**前置阅读**：`skills/hybrid_research_qa.py` L1079-1174（`_synthesize` 方法）、D盘 `agent/core.py` 的 `_sanitize_structured_answer`、`_guardrails`、`_validate_citations`

### 8.1 当前代码问题

- LLM 回答中可能出现内部引用编号（`[mysql-xxx]`、`[derived-xxx]`）、内部表名（`eln_sample`）、SQL 片段
- 实体对齐粒度错误：样品名（EXP-xxx）被误判为项目名，产生"项目名称冲突"错误判定
- 分析类回答可能出现因果断言（"导致""因为……所以……"）
- 没有自动注入假设/限制提示

### 8.2 改动方案

在 `skills/hybrid_research_qa.py` 中新增 `_sanitize_answer(answer, frame, scenario_id)` 方法，在 `_synthesize` 返回后、`answer()` 返回前调用。

#### 改动 1：内部信息清除

```python
import re

def _sanitize_answer(self, answer, frame, scenario_id):
    # 1. 剔除内部引用编号 [mysql-xxx] [derived-xxx] [vector-xxx] [evf-xxx]
    answer = re.sub(r'\[(?:mysql|derived|vector|evf|upload|dialog)-[a-f0-9]{8,}\]', '', answer)
    
    # 2. 剔除内部表名（在非代码块上下文中）
    table_names = ['eln_sample', 'sample_materials', 'data_column', 'mat_project', 
                    'eln_synthesis_exp', 'eln_verify_item', 'eln_verify_exp', 'archive_data']
    for table in table_names:
        answer = answer.replace(table, '业务数据表')  # 或直接删除
    
    # 3. 剔除执行计划前缀
    answer = re.sub(r'^(我需要|我将|我先|我打算|接下来我)[^\n]*\n', '', answer)
    
    # 4. 剔除 SQL 片段（SELECT ... FROM ...）
    answer = re.sub(r'SELECT\s+.*?\s+FROM\s+[^\n;]+[;]?', '', answer, flags=re.IGNORECASE|re.DOTALL)
    
    return answer
```

#### 改动 2：错误冲突判定修正

在 `agent/evidence_frame.py` 中修正实体对齐粒度（根因已核验在 `_walk_entity` / `_ENTITY_ID_KEYS`，不是 `evidence_relation.py`）：
- 样品名（EXP-xxx 格式）的 `entity_type` 应为 `"sample"`，不是 `"project"`
- 项目 ID（纯数字，如 -1360）的 `entity_type` 才是 `"project"`
- `_walk_entity` 当前把含 `project_id`+`name` 的样品行误判为项目实体（实测 1689 条 `project_id` 记录、5 组假冲突），需让 `project_id` 只用于项目实体的显式锚定，不吞并同行的样品 `name`

#### 改动 3：guardrails 自动注入假设

在 `_sanitize_answer` 末尾，根据场景类型自动追加假设/限制：

```python
    # 分析类场景自动追加因果提示
    if scenario_id in {8, 9, 10, 11}:
        if '不能据此判定因果' not in answer and '相关性' not in answer:
            answer += '\n\n> 注：以上为基于历史数据的相关性分析，不能据此直接判定因果关系。'
    
    # 含单位数值自动标注
    if scenario_id in {1, 2, 3, 4, 5, 6}:
        if '单位' not in answer:
            answer += '\n\n> 注：配方和工艺数值为原始字段单位，未做物理换算。'
    
    # 对照样品未指定时
    if scenario_id == 4 and '对照' not in answer:
        answer += '\n\n> 注：对照样品未经用户指定，结论仅供参考。'
```

#### 改动 4：引用校验（扩展到所有场景）

现有 `_validate_citations`（L925-936）只在阶段四场景（8,9,10,11,18）调用。扩展到所有场景：
- 如果回答中引用了 record_id，必须是 EvidenceFrame 中真实存在的
- 如果引用了不存在的 record_id，剔除该引用
- 查询类场景不强制要求引用（因为数据在卡片里）

### 8.3 验收标准

1. 回答中无内部引用编号（`[mysql-xxx]` 等）
2. 回答中无内部表名、SQL 片段
3. 无"项目名称冲突"错误判定
4. 分析类回答含"不能据此判定因果"提示
5. 三个实测回答中无内部信息泄漏

### 8.4 验证方法

- 调用三个实测问题，检查回答文本中是否还有内部编号/表名
- 检查 `evidence_frame.conflicts` 中是否还有把样品名当项目名的冲突

---

## 9. P0-6：向量检索修复

**优先级**：高（6 个实测全部 `向量库 0`，是独立根因，与 P0-2 并列）
**改动文件**：`skills/hybrid_research_qa.py`（`_vector_query`）、`agent/vector_search.py`（阈值与降级）
**前置阅读**：`skills/hybrid_research_qa.py`（`_vector_query` 方法）、`agent/vector_search.py`（`VectorSearchService.search`）

### 9.1 当前代码问题

- 长句直接作为 `queryText` 发往外部向量 API，语义被稀释，6 个实测场景 score 全部低于阈值被过滤
- 向量不可用或全低于阈值时，只输出 `vector_unavailable` 提示，没有重试词或降级策略

### 9.2 改动方案

#### 改动 1：查询词提取（覆盖全部20场景）

`_vector_query` 从问题中提取核心词（样品 ID、材料名、性能名、场景关键词），不发送整句。

**当前状态**：已实现场景 1-6、12、13、14、17（共9个）的查询词提取；场景 8-11、15、16、18、19、20（共11个）直接返回 `default_query`（整句），需补全。

补全规则：
- 场景 8/10（关键变量/冲突分析）：提取性能指标名（如"密度差""持液量"）
- 场景 9（工艺窗口）：提取性能指标名 + 阈值
- 场景 11（批次差异）：提取项目名/样品名
- 场景 15/16（结果关联/图谱复用）：提取样品 ID + 图谱类型（如"DSC""粒径分布"）
- 场景 18（阶段总结）：提取项目名 + 时间范围
- 场景 19（实验回流）：提取样品 ID + 实验类型
- 场景 20（跨项目复用）：提取项目名 + 关键词

#### 改动 2：阈值与降级

在 `VectorSearchService.search` 中：
- 全量低于 `score_threshold` 时，记录 `threshold_filtered_count`，并在 EvidenceFrame 中显式标记"向量证据缺口"
- 不自动把低分向量文档提升为结构化事实

### 9.3 验收标准

1. 三个实测问题的向量命中 > 0（有数据时），或明确标注"当前公司范围无向量证据"
2. 不发送整句作为 `queryText`
3. 向量缺口不阻断 MySQL 结构化回答

### 9.4 验证方法

- 检查 `search_vector_knowledge` 调用日志中的 `query` 字段是关键词而非整句
- 三个实测问题重跑，观察 `vector=ok` 或明确缺口

---

## 10. P1 体验优化（P0 完成后实施）

| # | 项 | 改动文件 | 具体改动 | 工作量 |
|---|---|---|---|---|
| P1-1 | 引用人类可读化 | `schemas/evidence.py` + `MarkdownView.jsx` | EvidenceRecord 增加 `display_label`（样品名+字段名），LLM 用 display_label 引用，前端点击展开样品卡片 | 低 |
| P1-2 | 前置记录归并+卡点 | `frontend/src/progress.js` + `App.jsx` | 14+内部节点归并为5~6用户可读阶段（连接通道→读取结构化证据→检索向量证据→证据对齐→生成报告），内部节点下沉详情；单步超15s高亮、30s提示；完成态加"依据：MySQL N·向量 M·冲突 K" | 低 |
| P1-3 | 省略透明化 | `App.jsx` EvidenceFrameCard | 用 `selection.omitted_records` 渲染"按相关度省略N条，不影响结论" | 低 |
| P1-4 | 答案默认展开 | `App.jsx` Message | P0-1 修复后自然解决，确认无折叠逻辑 | 低 |
| P1-5 | 卡片分组 | `App.jsx` Message | bubble 内卡片分组：回答正文组（MarkdownView+DataCards）、证据组（EvidenceFrameCard+ResearchScenarioCard）、执行详情组（Detail），用分隔线区分 | 低 |
| P1-6 | 证据卡默认折叠降噪 | `App.jsx` Message + `EvidenceFrameCard.jsx` | 原始证据记录默认折叠，主展示是 DataCards；用户主动展开才看 1532 条原始记录，避免编号噪音 | 低 |
| P1-7 | warnings 提升展示 | `App.jsx` Message + `DataCards.jsx` | 当前 warnings 只在 EvidenceFrameCard 里显示前5条，证据卡折叠后用户看不到。改为在数据卡片下方展示 warnings（如"向量库无相关文档""工艺参数缺失已降级"），不藏在折叠区 | 低 |

---

## 11. P2 引擎接入预留（独立，低优先级）

| # | 项 | 改动位置 | 具体改动 |
|---|---|---|---|
| P2-1 | 回答结构预留 `engine_results: []` | `hybrid_research_qa.answer()` 返回值 | 新增空字段，后续引擎结果直接进槽位，不改 LLM 综合逻辑 |
| P2-2 | progress stage 预留 `engine_*` 前缀 | `runtime/progress.py` 命名规范 | 文档约定，引擎接入时 AnalysisProgress 自动识别 |
| P2-3 | 图表 `chart_type` 枚举预留 | `engine/visualization/builder.py` | 预留 line/pareto/convergence 扩展位，后续加类型不改数据结构 |

---

## 12. 回复正文策略（P0 完成后生效）

| 场景类型 | 场景编号 | 正文策略 | 数据载体 |
|---|---|---|---|
| 查询类 | 1,2,3,5,6,7,12,13,15,16,20 | 2-4句总结+推荐（结果数量、推荐及理由、风险提示） | P0-3 数据卡片（TopN样品KV表） |
| 分析类 | 4,8,9,10,11,17 | 2-4句结论+关键数据+推理 | 证据卡片+引用 |
| 方案类 | 14,18,19 | 结构化模板（多套梯度方案） | 模板+LLM补充分析 |

**所有场景统一**：内部过程（SQL、表名、workflow名、内部ID编号）不进正文——手册硬约束。

---

## 13. 总验收清单

### 13.1 功能验收（全部通过才算完成）

- [ ] **P0-1**：三个实测问题回答完整可见，标题后正文不丢失，多段文本独立显示
- [ ] **P0-2**：聚合器成为 LLM 输入唯一主路径，压缩器只做尺寸兜底；回答中出现具体配方/工艺/性能数值；EvidenceFrame 与 LLM 输入解耦；SYSTEM PROMPT 已同步修改（无 record_id 标注要求、查询类场景2-4句输出）
- [ ] **P0-3**：问"EXP-128 相似配方"，回答下方出现 Top5 样品卡片，含配方/性能/推荐；问"密度差>100且持液量<0.1"，出现 Top5 合格样品卡片
- [ ] **P0-4**：参照样品工艺字段为空时，返回配方相似结果+缺失提示，不直接失败；工艺字段完整时无回归
- [ ] **P0-5**：回答中无内部引用编号、表名、SQL；无"项目名称冲突"错误判定；分析类回答含因果提示
- [ ] **P0-6**：向量查询发送核心词而非整句（覆盖全部20场景）；三个实测问题向量命中>0或明确标注缺口

### 13.2 渲染验收

- [ ] 标题、段落、列表、表格、代码块、引用块渲染正常
- [ ] 数据卡片样式正确，KV 行可读，推荐等级颜色区分
- [ ] 前置记录折叠/展开正常，卡点高亮

### 13.3 手册合规验收

- [ ] 数值均来自 Tool，无编造（手册约束1）
- [ ] 证据分类展示（手册约束2）
- [ ] 无因果断言（手册约束3）
- [ ] 数据不足明确说明（手册约束4/9）
- [ ] 回答不暴露内部过程（手册约束5）
- [ ] RAG 不只用向量，MySQL+向量联合（手册约束7）

### 13.4 验证方法

1. **后端接口测试**：用 curl 或 Python 调用 `POST /agent-api/api/v1/chat-ui`，传入三个实测问题，检查返回的 `answer`、`evidence_frame`、`data_cards` 字段
2. **前端渲染测试**：启动前端，在聊天框输入三个实测问题，观察渲染
3. **单元测试**：为 P0-1（MarkdownView）、P0-4（相似度降级）、P0-6（向量查询词提取）编写单元测试
4. **回归测试**：确保现有测试（`tests/` 目录）全部通过

---

## 14. 实施顺序与依赖

```
第1步：P0-1 MarkdownView 渲染修复（无依赖，0.5天）
   ↓ 回答可见后才能验证后续
第2步：P0-2 聚合器主路径 + 压缩器降级（无依赖，1.5天）
   ├── 并行：P0-4 相似度降级链（1天）
   └── 并行：P0-5 回答后处理守卫（0.5天）
   └── 并行：P0-6 向量检索修复（0.5天）
   ↓ P0-2 完成后
第3步：P0-3 确定性数据卡片层（依赖P0-2，复用聚合器输出，2天）
   ↓ P0 全部完成
第4步：P1 体验优化（1天）
第5步：P2 引擎预留（0.5天）
   ↓
总计：约 7 天
```

**注意**：
- P0-1 必须单独先做，因为回答渲染有 bug 时后续改动无法验证
- P0-2、P0-4、P0-5、P0-6 可并行开发
- P0-3 依赖 P0-2 的聚合器主路径
- 每完成一个 P0 项，立即用三个实测问题验证，不要全部做完再测

---

*本手册结束。执行智能体按顺序执行，每完成一项更新验收清单状态。*
