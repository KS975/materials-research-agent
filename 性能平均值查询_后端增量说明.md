# 性能平均值查询 V0.1（后端增量）

## 解决的问题

原问题：

```text
所有样品的冲击强度的平均值是多少
```

旧版本会把它识别为 `unsupported_future_feature`。本增量新增高精度意图
`performance_statistics`，直接读取当前用户有权访问的业务 MySQL 样品并由后端
确定性计算平均值。

## 计算和权限规则

- 只读取当前 `company_id` 下、当前用户有权限访问的项目。
- 使用现有 `list_samples_for_analysis` 只读工具和 keyset 分页完整扫描。
- 只计算目标性能字段的有效数值记录。
- 缺失值、非数值和目标字段不唯一的记录不参与平均值，并分别计数。
- 多种单位不能混算。
- 同时存在“有单位的数值”和“未记录单位的数值”时也停止计算。
- 平均值由后端 `Decimal` 计算，DeepSeek 不生成 SQL，也不重新计算数值。
- 此意图在 LangGraph 一级路由中优先执行，不会再落入
  `unsupported_future_feature` 或旧的导入数据演示路由。

## 覆盖文件

将压缩包中的下列文件按原目录覆盖到服务器后端项目：

```text
agent/router.py
agent/deepseek_intent_router.py
agent/core.py
api/chat_ui.py
skills/material_intelligence.py
```

测试文件仅用于核验，可不部署：

```text
tests/unit/test_material_intelligence_round2a2.py
tests/unit/test_round2a2_1_chat_ui_mysql_precedence.py
```

## 部署

本次没有前端改动，不需要替换前端 `dist`，也不需要修改 `.env`。

覆盖文件后重新构建并重建后端容器：

```bash
docker build --no-cache -t materials-agent:1.0 .
docker-compose up -d --no-deps --force-recreate mat-agent
```

随后检查：

```bash
docker-compose ps mat-agent
docker-compose logs --tail=100 mat-agent
curl -i http://127.0.0.1:18000/health
```

## 验收问题

```text
所有样品的冲击强度的平均值是多少
冲击强度平均大概多少？
冲击强度的均值是多少？
```

正确结果应明确显示：平均值、单位、扫描样品数、有效数值数、缺失数、非数值数，
并显示数据库分页扫描是否完整。

## 测试结果

- Python 静态编译：通过。
- 新增平均值计算、单位冲突、全量 575 条分页、路由优先级测试：通过。
- 相关 Router、Intent、材料分析和 LangGraph V1-V4 回归：116 项通过。
- 完整 `tests/unit` 中依赖未随当前源码包提供的 `.runtime` 演示数据测试不在本次
  增量验收范围内。
