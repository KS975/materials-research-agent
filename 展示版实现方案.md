# 材数智元数据底座问答能力展示版实现方案

日期：2026-09-08

## 1. 定位与结论

本方案面向“材数智元数据底座建好后，可直接体现价值的 20 个具体场景”，将当前系统升级为可展示、可验收的混合研究问答能力。

核心结论：

1. 20 个场景是强制需求，不做功能裁剪；但不能实现成 20 个孤立 Tool，应按真实执行流程归并为 8 类可复用 Workflow。
2. 数据来源边界固定为三类：外部 MySQL、外部向量库、前端上传与对话输入。LIMS、图谱、曲线、竞品资料、报告和案例不是第四类独立数据源，只能作为这三类来源中的数据形态或派生特征。
3. 外部 MySQL 与外部向量库均由后端 Tool 调用；智能体不能先分别生成两段答案再拼接，而应在同一个 Workflow 中并行召回、统一成 EvidenceFrame 后综合分析。
4. 向量库不是 Qdrant，也不由本系统建设维护；本系统只负责通过外部向量检索 API 调用。当前系统已有 Qdrant 直连实现，但尚未形成通用外部向量 API 适配层。
5. 部分结构化数据隐藏在向量库中的抽取与合并属于进阶能力，本期只预留 Skill / Workflow 节点，不做正式实现。
6. 当前系统已有 Intent Router、Skill Registry、Scenario Workflow、Tool Registry、MySQL 只读链路和平台请求头代码；需要补齐的是 Hybrid Research QA、EvidenceFrame、外部向量 API 统一适配和场景级验收。

## 2. 需求场景与 Workflow 归并

分类依据不是场景名称，而是实际执行流程、输入输出契约、数据副作用和权限风险。相同 Workflow 必须满足：数据来源相近、处理步骤可复用、输出形态接近、审计与写入风险一致。

| Workflow | 覆盖场景 | 输入 | 主要执行流程 | 输出 |
|---|---|---|---|---|
| 混合检索与排序 | 1 相似配方检索；2 相似样品/实验检索；3 按目标性能反查历史方案；5 原料使用效果查询；6 原料替代历史检索；7 失败配方/实验检索；12 异常与失效案例检索；13 竞品对标 | 样品、配方、目标性能、原料、失效现象、竞品特征 | 权限过滤 → 外部 MySQL 召回 → 外部向量库召回 → EvidenceFrame 合并 → 相似度/条件过滤 → 排序 | 候选样品/配方/案例列表，含相似度、差异、证据来源 |
| 证据联查与画像 | 4 配方-工艺-性能联查；17 项目知识快速问答 | 样品、实验、项目、原料、问题 | 实体锚定 → 结构化事实联查 → 非结构化证据联查 → 时间/单位/实体对齐 → 综合解释 | 样品/项目完整画像、项目知识回答、证据链 |
| 新项目冷启动 | 14 新项目冷启动 | 目标性能、成本、原料限制、工艺限制 | 目标解析 → 混合检索 → 失败记录检索 → 竞品/文献检索 → 组合首轮方案 | 起始配方方向、可参考历史项目、风险与缺口 |
| 结果自动关联与特征接入 | 15 测试结果自动关联样品；16 图谱/曲线结果复用 | 来自外部 MySQL、外部向量库或前端上传的检测结果、图谱/曲线文件及特征 | 读取三类来源 → 样品/实验匹配 → Schema 校验 → 特征抽取 → 冲突检测 → 待审核登记 | 关联结果、特征表、待审核记录、失败原因 |
| 历史数据分析 | 8 关键变量识别；10 多性能冲突分析；11 批次差异分析 | 目标性能、批次组、候选变量 | 构建 Evidence Dataset → 统计分析/模型解释 → 冲突与差异归因 → 输出证据 | 关键变量排名、性能权衡、批次差异原因 |
| 配方/工艺窗口发现 | 9 配方/工艺窗口发现 | 多性能目标、约束、变量范围 | 历史候选筛选 → 可行域计算 → 稳健区间评估 → 边界与风险分析 | 稳定配方/工艺窗口、边界条件、不满足原因 |
| 阶段总结生成 | 18 自动生成阶段总结 | 项目、时间段、实验范围 | 汇总实验、结果、结论、文档证据 → 结构化大纲 → 报告生成 → 引用校验 | 研发阶段报告、关键结论、证据清单 |
| 模型与实验闭环 | 19 模型版本与实验回流；20 跨项目复用 | 新实验结果、模型版本、目标项目 | 实验回流识别 → Dataset 新版本 → Challenger 训练 → 审批晋级 → 资产索引 → 授权复用 | Dataset/Model 新版本、回流报告、可复用资产清单 |

说明：

1. 场景 14 是组合型场景，复用混合检索、证据联查和失败案例检索，不单独复制实现。
2. 场景 15、16 涉及外部结果读取、特征登记和写入审核，必须与只读问答分离。
3. 场景 19 涉及 Dataset 新版本、模型训练和审批晋级，不能与普通问答共用无写入 Workflow。
4. 20 个场景可以作为 20 个入口意图或意图别名，但底层复用 8 类 Workflow，避免 Tool 数量失控。

## 3. 目标架构

```text
React / SSE 前端
  对话、过程状态、任务状态、证据卡、图表数据、最终报告
        ↓
FastAPI Gateway
  请求校验 / 身份权限 / SSE / API 版本 / 审计入口
        ↓
Agent Runtime
  Intent Router
  Skill Registry
  Scenario Workflow Composer / Orchestrator
  Evidence Builder / Entity Resolver / Conflict Resolver
  Tool Registry
        ↓
数据来源与派生能力 Tool
  外部 MySQL 查询 Tool
  外部向量检索 / 索引 Tool
  前端上传与对话输入 Tool
  图谱 / 曲线 / 表格 / 案例特征解析 Tool
  预处理 / 建模 / 预测 / 优化 Tool
        ↓
数据与运行时层
  外部 MySQL 只读 / 外部向量库 / 前端上传对象存储 / Dataset Registry /
  Model Registry / Experiment Registry / Runtime DB / 对象存储
        ↓
横切能力
  权限 / 审计 / 血缘 / 日志监控 / 配置密钥 / 审批 / 安全护栏
```

架构调整原则：

1. 不推翻现有 Intent Router、Skill、Workflow、Tool 分层。
2. 不让 LLM 直接生成 SQL，也不让 LLM 绕过 Tool Registry 访问外部系统。
3. 不由前端直接调用外部数据源；所有外部接口均由后端 Tool 调用。
4. 不新建“向量库隐藏结构化数据接口层”；本期只在 Hybrid Workflow 中预留抽取与实体归一节点。
5. 执行过程只输出结构化状态、证据和图表数据；任务完成后输出完整报告。

## 4. 意图路由、Skill、Workflow、Tool 分级

| 层级 | 职责 | 禁止事项 |
|---|---|---|
| Intent Router | 识别用户场景、槽位、上下文引用和不确定性；不确定来源时默认进入 Hybrid Research QA | 不直接查库，不生成 SQL，不决定权限 |
| Skill | 定义业务能力边界、支持的 operation、输入输出契约、Tool 白名单、证据规则、护栏和审批点 | 不执行多步编排，不直接访问外部接口 |
| Workflow | 固定或半固定 DAG，负责跨 Tool 编排、上下文传递、EvidenceFrame 构建、失败关闭和结果组装 | 不绕过权限、审计和审批 |
| Tool | 调用一个外部接口或原子能力，做输入 Schema 校验、输出规范化、错误转换和审计 | 不做跨源综合判断，不隐藏副作用 |
| Evidence Service | 实体对齐、单位换算、冲突标记、证据归并、血缘记录 | 不篡改原始来源，不默认丢弃冲突 |

当前已有能力与需要新增能力：

| 能力 | 当前状态 | 目标调整 |
|---|---|---|
| Intent Router | 已有确定性路由和 DeepSeek 语义路由 | 新增混合研究问答意图和 20 场景别名；不确定数据来源时默认 Hybrid QA |
| Skill Registry | 已有 Skill 契约和权限/护栏定义 | 新增 Hybrid Research QA、Result Ingestion、Feature Ingestion 等 Skill |
| Scenario Workflow | 已有场景 DAG 和 Engine Task | 新增 8 类研究问答 Workflow |
| Tool Registry | 已有 JSON Schema、权限、审计 | 新增外部 MySQL、外部向量 API、上传/对话输入、图谱曲线特征解析 Tool |
| Joint MySQL+Knowledge | 已有联合分析雏形 | 升级为 EvidenceFrame 层融合，避免回答级拼接 |
| Engine | 已有预处理、建模、预测、优化 | 通过 Evidence Dataset Adapter 接入统一证据数据 |

## 5. EvidenceFrame 统一证据模型

Hybrid Research QA 的核心是先把不同来源的结果转换为统一 EvidenceFrame，再做联合分析。

```text
外部 MySQL 查询结果
外部向量库召回结果
前端上传与对话输入
图谱 / 曲线 / 表格 / 案例派生特征
        ↓
Evidence Record
        ↓
实体对齐 + 单位对齐 + 时间对齐
        ↓
冲突标记与权威级别判定
        ↓
联合分析 / 建模 / 最终报告
```

建议 Evidence Record 字段：

| 字段 | 含义 |
|---|---|
| `record_id` | 证据记录 ID |
| `subject_id` | 样品、实验、项目、原料、牌号等主体 ID |
| `entity_type` | 主体类型 |
| `attribute` | 属性或字段名 |
| `value` | 数值、类别或文本值 |
| `unit` | 单位 |
| `observed_at` | 观察/实验/检测时间 |
| `source_type` | mysql / vector_api / upload / dialog / derived |
| `source_uri` | 外部系统或文档地址 |
| `chunk_id` | 向量片段 ID，可为空 |
| `confidence` | 置信度 |
| `authority_level` | 权威级别 |
| `conflict_group` | 冲突组 ID |
| `review_status` | AUTO / REVIEWED / REJECTED |
| `permission_scope` | company/project/user 范围 |

权威级别建议：

```text
外部 MySQL 权威记录
  > 已审核报告表格
  > 普通文档表格
  > LLM 从自由文本抽取的事实
```

实体对齐分级：

```text
L1 精确 ID：sample_id / experiment_id / project_id / material_id
L2 强上下文：项目 + 名称 + 时间 + 类型 + 批次
L3 字段匹配：规范化字段名 + 单位和量纲一致
L4 模糊匹配：名称相似、时间缺失、单位缺失或多个候选
```

L1、L2、L3 可自动参与分析和排序；L4 只能作为提示性线索，输出不确定性并等待用户确认，不得直接作为结论依据。

冲突处理规则：

1. 不同来源数值不一致时，不静默覆盖。
2. 默认展示权威来源，同时列出冲突来源和差异。
3. 未审核的抽取事实不得直接进入正式建模 Dataset。
4. 用于建模的数据必须能追溯到 Dataset Version、来源记录和审核状态。

建模数据准入：

| 来源 | 可否直接进入正式 Dataset | 处理方式 |
|---|---|---|
| 外部 MySQL 权威记录 | 可以 | 字段目录、单位和权限过滤后进入 |
| 已审核前端上传数据 | 可以 | Schema、单位、实体对齐和人工审核后进入 |
| 外部向量库原文事实 | 默认不可以 | 作为回答证据；需人工审核后才可进入 Dataset |
| LLM 抽取事实 | 不可以 | 仅生成候选事实并进入审核队列 |
| 对话中的临时约束 | 不可以 | 只作用于本轮查询或优化，不写入训练数据 |

进入 Dataset 的 Evidence Record 必须保留 `source_type / source_uri / permission_scope / review_status / dataset_version`，保证样本可追溯、可撤销。

## 6. 混合研究问答执行流程

```text
用户问题
  ↓
意图与槽位识别
  ↓
路由短路判断
  ├─ 明确样品/实验/项目 ID → 直接外部 MySQL 画像
  ├─ 明确目标性能阈值 → 优先结构化筛选，再补充向量证据
  └─ 来源不确定 → 默认混合检索
        ↓
Hybrid Research QA Skill
  ↓
并行证据召回
  ├─ 外部 MySQL 查询 Tool
  ├─ 外部向量检索 Tool
  └─ 前端上传 / 对话上下文 Tool
        ↓
图谱 / 曲线 / 表格 / 案例特征解析
  ↓
统一 EvidenceFrame
  ↓
权限过滤 / 实体对齐 / 单位换算 / 时间对齐
  ↓
场景分析节点
  ├─ 相似度计算
  ├─ 条件筛选
  ├─ 关键变量分析
  ├─ 批次差异分析
  ├─ 窗口发现
  └─ 报告生成
  ↓
最终综合报告 + 证据链 + 图表数据
```

关键约束：

1. 不要求用户说明“这是结构化问题还是非结构化问题”。
2. 不允许先输出一段 MySQL 答案、再输出一段 RAG 答案。
3. 所有跨源结论必须引用 EvidenceFrame 中的 record_id。
4. 找不到证据时明确说明缺口，不得编造历史实验或性能结论。
5. 涉及写入、建模、回流时进入对应审批和检查点。
6. 短路路径只减少不必要的外部调用，不改变 EvidenceFrame 输出契约。
7. 混合检索、窗口发现和报告生成等长任务使用异步 Engine Task，执行中只输出阶段状态和图表数据。

## 7. 向量库隐藏结构化数据预留设计

本期不实现从向量库内容中抽取隐藏结构化数据，但在 Workflow 中预留节点：

```text
Hybrid Research QA Workflow
  → 混合召回
  → EvidenceFrame Builder
  → VectorFactExtractor（预留，本期空实现或返回 NOT_SUPPORTED）
  → EntityResolver（基础实体对齐本期实现）
  → ConflictResolver（冲突标记本期实现）
  → 综合分析
```

后续启用条件：

1. 外部向量 API 能返回原文、表格、片段和 metadata。
2. 明确可抽取实体类型和字段 Schema。
3. 明确样品、实验、项目、原料等实体匹配键。
4. 明确单位、范围、置信度和人工审核规则。
5. 抽取事实进入建模前必须形成 REVIEWED Dataset。

## 8. 数据来源、接口与请求头现状

### 8.1 来源边界

目标系统只允许三类原始数据来源：

```text
1. 外部 MySQL：权威结构化事实
2. 外部向量库：非结构化文本、案例、报告、表格片段和向量结果
3. 前端上传与对话输入：用户显式提供的数据、文件、图谱、曲线和约束
```

LIMS、图谱、曲线、竞品资料、报告和案例不新增独立外部数据源。它们只能来自：

```text
外部 MySQL 中已同步的结构化记录
外部向量库中已索引的内容
前端上传文件或对话输入
```

系统可以在此基础上做特征抽取、实体对齐和派生数据登记，但派生数据的血缘必须指向上述三类原始来源。

### 8.2 外部数据源契约矩阵

| 契约项 | 外部 MySQL | 外部向量库 | 前端上传与对话输入 |
|---|---|---|---|
| 鉴权方式 | 后端受管只读账号，不转发浏览器 Authorization | 外部向量 API 服务凭证或网关交换凭证 | 本系统平台身份，UserContext 控制归属 |
| 权限过滤 | company_id / project_id 强制进入查询条件 | 请求 metadata 过滤，返回后二次校验 | 上传时绑定归属，访问时二次校验 |
| 查询能力 | 参数化只读查询、分页、排序、聚合 | 向量检索、混合检索、metadata 过滤 | 附件读取、对话槽位、显式约束 |
| 返回要求 | 字段、值、单位、记录 ID、更新时间 | score、chunk_id、原文片段、metadata、来源 | 文件 URI、解析结果、用户声明来源 |
| 失败降级 | 查询失败则失败关闭，不用向量结果冒充结构化事实 | 向量不可用时提示证据缺口，可保留 MySQL 事实回答 | 解析失败则拒绝进入 EvidenceFrame |
| 缓存策略 | 只缓存 Schema 和短 TTL 查询结果 | 可缓存查询向量与结果摘要，不缓存敏感原文 | 会话内缓存，历史会话按权限读取 |
| 审计要求 | SQL 意图、工具名、行数、耗时、错误类型 | 查询语句、top_k、过滤条件、命中数、score 阈值 | 文件名、解析器、大小、归属、审核状态 |

外部向量 API 必须明确是否支持写入索引。若支持，前端上传内容可在解析后按外部 API 契约写入索引，并保留本系统对象存储 URI 作为血缘；若不支持，上传内容只参与当前会话和本地派生特征，不承诺跨会话向量检索。

### 8.3 当前状态

| 项目 | 当前状态 | 结论 |
|---|---|---|
| 外部 MySQL | 已接入真实只读库，具备公司/项目过滤 | 本地链路可用 |
| 平台请求头 | 前端 `authorization / company-id / organization-id / organization-level` 代码已存在；后端 `PlatformPermissionAdapter` 已存在 | 代码已写好，但当前 `.env` 仍为 `development_header`，平台模式未正式启用 |
| 向量库 | 当前代码主要面向 Qdrant 直连 | 不满足“外部向量库只调用 API”的要求，需要适配层 |
| 前端上传与对话输入 | 已有上传解析和对话上下文，但尚未统一进入 EvidenceFrame | 需要补充 Evidence Record 转换 |
| 图谱 / 曲线 / LIMS 数据 | 尚未作为三类来源中的数据形态统一解析和登记 | 需要特征解析与血缘规则 |
| 外部向量 API Gateway | 尚未形成统一服务端转发层 | 需要新增 |

### 8.4 目标请求链路

```text
浏览器
  ↓ 同源 /agent-api/
FastAPI Gateway
  ↓ 解析平台身份，生成 UserContext
Tool Registry
  ├─ 外部 MySQL 查询 Tool
  │    ↓ 只读连接 / 参数化查询 / 公司项目过滤
  ├─ ExternalVectorAPIGateway
  │    ↓ 外部向量检索 / 索引 API
  └─ 上传与对话输入 Tool
       ↓ 对象存储 / 附件解析 / 会话槽位
```

前端只负责把单位平台登录态传给同源 `/agent-api/`，不直接访问外部 MySQL 或外部向量库。

后端调用外部向量 API 时统一附加：

| 信息 | 用途 |
|---|---|
| 平台用户 Authorization 或外部向量服务凭证 | 外部向量 API 鉴权 |
| `acting_user_id` / `company_id` / `project_scope` | 数据权限与审计 |
| `trace_id` / `workflow_id` / `tool_call_id` | 全链路追踪 |
| 请求超时、重试、限流策略 | 稳定性 |

外部向量 API 的实际 Header 名称按其接口契约映射。平台用户模式下，请求头 `Company-Id` 必须保持当前登录公司；`EXTERNAL_VECTOR_QUERY_COMPANY_ID` 仅用于平台授权的测试数据范围，生产环境必须留空。外部 MySQL 使用后端受管只读账号和参数化查询，不转发浏览器 Authorization；平台 Authorization 只用于本系统身份解析和外部向量 API 调用。上传与对话输入只通过本系统 API 进入，并由 UserContext 限定归属。

### 8.5 前端上传与对话输入链路

```text
用户上传文件 / 输入约束
  ↓
FastAPI Gateway
  ↓ 权限校验、大小限制、类型限制、安全检查
对象存储与附件解析
  ↓
结构化抽取 / 表格解析 / 图谱曲线特征解析
  ↓
Evidence Record
  ├─ 当前会话参与问答
  ├─ 派生特征进入待审核登记
  └─ 若外部向量 API 支持索引写入，则按权限写入外部向量库
```

上传内容默认不进入正式 Dataset。只有通过 Schema 校验、单位校验、实体对齐和人工审核后，才可用于建模；对话约束只作用于本轮任务，不自动沉淀为训练数据。

## 9. 实施计划

### 阶段一：接口与身份基座

目标：先保证三类数据来源调用和权限链路可信。

1. 梳理外部 MySQL 只读查询契约和外部向量 API 的认证、检索、索引、响应契约。
2. 实现 `ExternalVectorAPIGateway`：服务凭证、用户上下文、Trace ID、超时、重试、审计。
3. 将外部向量检索从 Qdrant 直连迁出到可配置 API Tool。
4. 将前端上传和对话输入统一转换为带归属与血缘的 Evidence Record。
5. 启用平台模式前完成真实网关验收：Token 验证、company-id 与用户归属校验、越权测试。
6. 建立外部数据源契约矩阵和降级策略，确认外部向量 API 是否支持索引写入、metadata 过滤和原文返回。

验收：

```text
三类来源均由后端 Tool 进入 EvidenceFrame
所有调用带 UserContext 和审计
平台请求头在真实平台可用
跨公司/跨项目越权失败
外部数据源失败降级符合契约矩阵
```

### 阶段二：EvidenceFrame 与混合检索

目标：实现统一证据层，解决“两来源分别回答后拼接”的问题。

1. 定义 Evidence Record / EvidenceFrame Schema。
2. 实现外部 MySQL 结果、外部向量结果、上传与对话输入到 Evidence Record 的转换。
3. 实现基础实体对齐、单位换算、时间对齐、冲突标记。
4. 新增 Hybrid Research QA Skill 和混合检索 Workflow。
5. 前端增加证据结果卡，展示结构化命中、非结构化证据、冲突和来源。
6. 实现路由短路规则：明确 ID 走 MySQL 画像，明确性能阈值优先结构化筛选，来源不确定才全量混合召回。
7. 将长查询接入异步 Engine Task，并行召回、有限等待、分页合并，避免前端请求超时。

验收：

```text
同一报告内同时引用 MySQL 与向量 API 证据
明确 ID 查询不强制发起向量召回
跨源实体能按样品/实验/项目对齐
冲突不会被静默覆盖
无证据结论为 0
长查询可异步执行并输出阶段状态
```

### 阶段三：只读研究场景落地

覆盖场景：1、2、3、4、5、6、7、12、13、14、17。

1. 实现相似配方、相似样品、相似实验的统一相似度策略。
2. 实现目标性能条件反查和候选排序。
3. 实现原料使用效果、替代历史、失败案例、异常案例、竞品对标。
4. 实现配方-工艺-性能联查和项目知识问答。
5. 新项目冷启动复用以上能力并组装首轮方案。

验收：

```text
每个场景至少 3 个真实问题样例
返回候选、相似度/筛选依据、证据来源和边界说明
20 场景追踪矩阵全部标记
```

### 阶段四：分析与报告场景

覆盖场景：8、9、10、11、18。

1. 从 EvidenceFrame 构建 Evidence Dataset。
2. 实现关键变量识别、多性能冲突分析、批次差异分析。
3. 实现配方/工艺稳定窗口发现。
4. 实现阶段总结报告生成和引用校验。

验收：

```text
分析结论能追溯到样本范围和证据记录
窗口发现同时输出可行区间和稳健性
阶段报告包含实验范围、结果、结论、风险和证据链
```

### 阶段五：结果接入、闭环与跨项目复用

覆盖场景：15、16、19、20。

1. 从外部 MySQL、外部向量库或前端上传内容中识别检测结果，并自动匹配样品、实验、配方。
2. 从上传文件、向量片段或 MySQL 记录中解析图谱/曲线特征，并关联样品与实验。
3. 实验回流识别：新增实验、推荐验证结果、历史补录。
4. 生成 Dataset 新版本并执行 Challenger 模型评估。
5. 模型晋级、废弃、回滚走人工审批。
6. 建立 Dataset、Model、Experiment、报告和经验的资产索引，支持授权跨项目复用。

验收：

```text
检测结果错配可发现、可审核
图谱特征可参与检索与分析
实验回流生成 Dataset 新版本且不覆盖旧版
模型晋级有审批和血缘
跨项目复用受公司/项目权限控制
```

## 10. 测试与验收矩阵

| 类型 | 内容 |
|---|---|
| 权限测试 | 公司隔离、项目隔离、跨公司拒绝、平台 Header 伪造拒绝 |
| 接口测试 | 外部 MySQL 查询、外部向量 API 超时、重试、错误映射、Schema 校验、审计 |
| 检索测试 | 相似配方、相似样品、目标性能反查、失败案例、竞品对标 |
| 证据测试 | 跨源引用、实体对齐、单位换算、冲突保留、来源可追溯 |
| 分析测试 | 关键变量、性能冲突、批次差异、窗口发现 |
| 写入测试 | 检测结果关联、图谱曲线特征接入、实验回流、Dataset 版本、模型审批 |
| 前端测试 | 过程状态、证据卡、图表数据、终态报告、错误与空结果 |
| 安全测试 | Token 不落盘、不输出、不进入聊天历史；业务库只读 |

20 个场景必须建立独立验收问题，并在追踪矩阵中记录：

当前追踪矩阵文件：`04_docs/research-scenario-acceptance-matrix.md`。矩阵必须区分单元测试通过、真实数据待验收、阶段四/五未执行，不得把预留 Workflow 标记为已交付能力。

```text
场景编号
入口问题
命中的 Intent / Skill / Workflow
调用的 Tool
返回的证据来源
是否发生写入
是否需要审批
测试结果
```

## 11. 风险与待定问题

| 风险 | 处理策略 |
|---|---|
| 外部向量 API 契约未明确 | 先定义内部 Tool 契约，外部实现通过 Adapter 配置 |
| 实体 ID 不稳定 | 结合样品 ID、实验 ID、项目 ID、名称、时间和上下文做实体解析，低置信度进入待确认 |
| 单位与字段不规范 | 建立字段和单位映射表；无法确认时不合并 |
| 向量结果包含隐藏表格 | 本期仅预留 VectorFactExtractor 节点，不进入正式建模 |
| 数据源冲突 | 保留冲突组和权威级别，不由 LLM 静默裁决 |
| 平台 company-id 可伪造 | 必须由可信网关校验用户与公司归属，不能只信任前端 Header |
| 场景间能力重复 | 以 Workflow 复用和追踪矩阵约束，不做重复 Tool |
| 长查询超时 | 使用 SSE 状态、异步 Task、检查点和分页召回 |

## 12. 最终交付形态

完成后，系统应形成以下能力：

1. 研发人员自然语言提问，无需区分外部 MySQL、外部向量库、上传文件或对话输入。
2. 系统自动并行调用外部 MySQL 与外部向量库，并合并上传/对话输入。
3. 跨源结果进入统一 EvidenceFrame，实体对齐后综合分析。
4. 相似检索、目标反查、失败案例、竞品对标、项目问答可直接展示价值。
5. 关键变量、冲突、批次差异和稳定窗口可解释、可追溯。
6. 检测结果、图谱曲线和实验结果可从三类来源审核接入，形成 Dataset 与模型闭环。
7. Dataset、Model、Experiment、报告和经验成为可授权复用的企业资产。
8. 全链路具备权限、审计、血缘、审批和回滚能力。
