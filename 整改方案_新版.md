# 材数智能体回答链路整改方案（新版）

> 指导思想：所有代码和模块的存在意义是"正确回答用户问题"，不是"展示系统做了什么"。根据回答目标确定代码、框架、路径和工具，不被现有代码捆绑。

---

## 执行说明（供执行智能体使用）

**项目路径**：`E:\Trae\Demo3\materials-research-agent`

**核心文件清单**（执行前必读）：
- 后端：`skills/hybrid_research_qa.py`（回答生成）、`agent/scenario_aggregator.py`（信息提取）、`agent/evidence_frame.py`（证据框架）、`agent/evidence_context.py`（压缩器）、`skills/material_intelligence.py`（相似度）
- 前端：`frontend/src/App.jsx`（Message组件L764-775）、`frontend/src/MarkdownView.jsx`（正文渲染）、`frontend/src/DataCards.jsx`（数据卡片）、`frontend/src/EvidenceFrameCard.jsx`（证据卡片）、`frontend/src/progress.js`（执行进展）

**执行顺序**：层次③ → 层次④ → 层次⑤ → 层次①② → 层次⑥

**验证方法**：每完成一个层次，用以下3个实测问题验证：
1. `查找与 EXP-128 相似的配方，并结合历史案例`
2. `找与 EXP-128 工艺最像的样品，并结合历史资料`
3. `查找密度差大于 100 且持液量小于 0.1 的历史样品，并结合历史资料`

**实施状态（2026-09-10）**：
- 层次①②③④⑤⑥ 的整改项均已完成代码落地。
- 后端定向回归 54 项通过；前端测试 21 项通过；前端构建通过。
- 真实链路冒烟已通过：`hybrid_research_qa` 能返回场景、证据维度、业务化缺失提示、TopN 数据卡片和统一警告。
- 执行详情写入 `.runtime/research_execution_audit.jsonl`，查询接口为 `GET /api/v1/research-execution-log`，按当前用户和公司隔离。
- 后续进入真实问题人工验收，重点观察答案事实质量、数据覆盖范围和不同真实数据分布下的提示是否准确。

---

## 整改思想（为什么这样改）

**第一性原理**：所有代码和模块的存在意义是"正确回答用户问题"，不是"展示系统做了什么"。根据回答目标确定代码、框架、路径和工具，不被现有代码捆绑。

**核心判断**：当前系统过多地展示了内部过程（扁平碎片、压缩器选择、审计表格、workflow名、companyId），而用户需要的是**结论**和**支撑结论的具体数据**。内部过程应该对用户不可见，只在后端日志中保留。

**关键决策及理由**：

| 决策 | 理由 |
|---|---|
| LLM输入不使用扁平碎片 | EvidenceFrame 扁平碎片保留（用于审计、引用校验、证据面板数据源），但 LLM 输入改用聚合器结构化摘要，避免从碎片重建结构 |
| 压缩器只做尺寸兜底 | "哪些信息重要"是信息提取层的职责，压缩器不应该做内容选择 |
| 聚合器扶正为信息提取核心 | 聚合器已经能按场景生成结构化摘要，是正确的方向，只是之前被"旁挂"了 |
| LLM只生成不改数值 | 数值必须来自确定性逻辑（数据库），LLM只负责组织自然语言，否则会编造或篡改 |
| 证据面板展示相关信息本身 | 用户需要的是具体配方/性能数值，不是record_id/source_type/confidence等审计字段 |
| 回答格式按意图自适应 | 查询类要简洁结论+推荐，分析类要推理过程，方案类要结构化方案，统一三段式不适应 |
| 执行详情移到后端日志 | 用户不需要知道companyId、API限制、workflow名，这些是开发者调试用的 |
| 分层混合提取 | 结构化数据→确定性逻辑（保证数值精确），文档→LLM摘要（需要语义理解），两者不互换 |

---

## 本次测试背景

**测试时间**：第一轮P0整改完成后
**测试环境**：项目 `E:\Trae\Demo3\materials-research-agent`，真实MySQL链路，向量库为外部API
**测试结果文件**：`D:\HuaweiMoveData\Users\linyu\Desktop\新建 文本文档.txt`（含6个场景的完整前端输出）

**测试的7个场景及核心问题**：

| # | 测试问题 | 场景 | 核心问题 |
|---|---|---|---|
| 1 | 查找与 EXP-128 相似的配方 | 相似配方检索 | 有数值了但推荐等级无区分（全是首选复用）；正文仍是三段式 |
| 2 | 找与 EXP-128 工艺最像的样品 | 相似样品检索 | 有降级了但结论说"原料组成最相似"，问工艺答原料，未明确说"没有工艺数据" |
| 3 | 找与 EXP-128 性能最像的样品 | 相似样品检索 | 结果和场景1完全相同，未按性能距离排序 |
| 4 | 密度差>100且持液量<0.1的样品 | 性能反查 | 数据卡片只有样品名+"可参考"，无配方无性能数值 |
| 5 | 查 EXP-128 完整研发上下文 | 配方-工艺-性能联查 | 笼统说"缺失完整研发上下文"，不说明有什么缺什么；说"合成验证次数为0"但有性能数据，逻辑矛盾 |
| 6 | 查水用在哪些样品 | 原料使用效果 | 卡片只有水的用量，无性能；水的用量和场景1差4个数量级（单位不一致） |
| 7 | 查找 P507+煤油 替代 水 的历史记录 | 原料替代检索 | 卡片只有样品名，无配方数值；正确判断了"仅共存不能证明替代" |

**第一轮已修复的问题**（本方案不再重复）：Markdown渲染（标题后正文丢失、段落堆积）、数值进LLM上下文、相似度降级链、实体对齐bug（样品名误判项目名）、向量查询词提取、内部编号清除。

**本方案解决的问题**：上述7个场景暴露的新问题 + 前端展示问题 + 架构层面的扁平碎片/压缩器越位问题，共41项。

---

## 一、目标架构

```
用户提问
  ↓
① 意图识别（问什么、需要什么维度的数据、回答格式类型）
  ↓
② 数据获取（结构化查询 + 向量检索，并行）
  ↓
③ 信息提取（按意图从原始数据中提取相关信息，保留结构化格式，确定性逻辑）
  ↓
④ 回答生成（LLM基于提取的信息 + 按意图自适应的回答规范，生成正文；LLM只组织语言不改数值）
  ↓
⑤ 前端展示
   ├── 执行进展（SSE，回答之前，5-6阶段，实时）
   ├── 引擎任务卡片（如有，含图表，回答之前，执行过程）
   ├── 回答正文（LLM最终报告，两段式：结论+风险提示）
   └── 证据面板（默认折叠，展开后=提取的相关信息本身）
```

**执行详情（审计表格、技术参数、companyId、workflow名）→ 后端运行日志，前端不展示。**

---

## 二、整改项（按架构层次组织）

### 层次①：意图识别

| 项 | 改动 | 解决的问题 |
|---|---|---|
| 1.1 意图输出扩展 | router 输出不仅是 scenario_id，还包含 `needed_dimensions`（配方/工艺/性能/文档）和 `answer_format`（查询/分析/方案） | 信息提取层知道该提取什么 |
| 1.2 缺失维度标注 | 意图识别后检查目标样品是否有 needed_dimensions 的数据，无则标注 `missing_dimensions` | 答非所问：问工艺答原料 |

### 层次②：数据获取

| 项 | 改动 | 解决的问题 |
|---|---|---|
| 2.1 向量查询词优化 | ~~`_vector_query` 覆盖全部20场景~~ **已完成**（P0-6 commit 4d6aa27）。需验证实际运行时 args 参数是否正确填充，若仍 fallback 到整句则修复 args 提取 | 向量检索全0（第一轮已部分修，补全剩余场景） |
| 2.2 向量阈值透明 | 向量全低于阈值时，明确标注"当前范围无相关文档"，不静默丢弃 | 用户不知道向量为什么没结果 |

### 层次③：信息提取（核心层，聚合器扶正）

| 项 | 改动 | 解决的问题 |
|---|---|---|
| 3.1 LLM输入改用聚合器摘要 | EvidenceFrame 扁平碎片**保留**（用于审计、引用校验、证据面板数据源），但 **LLM 输入不使用扁平碎片**，改用聚合器的结构化摘要。删除压缩器把扁平碎片送入LLM上下文的逻辑 | 压缩器把数值挤掉、LLM从碎片重建结构 |
| 3.2 聚合器扶正 | `scenario_aggregator` 从"旁挂派生记录"升级为 LLM 输入的唯一主路径，输出 `{samples, documents, missing_dimensions}` | 信息提取不彻底 |
| 3.3 压缩器降级 | ~~删除压缩器的"按分数挑选内容"逻辑~~ **已完成**（P0-2 commit c9f16ef，Compressor 只剩 bound_context 尺寸兜底）。确认聚合器摘要为 LLM 唯一主路径 | 压缩器职责越位 |
| 3.4 扩展Tool返回字段 | **根因在Tool层，不在聚合器**。`find_samples_multi_condition`（场景3性能反查）和 `_material_usage_scan`（场景6原料效果）当前只返回 sample 基本信息，不含 formula/performance。需扩展这两个Tool返回配方和性能字段，聚合器才能提取到数据 | 数据卡片场景4/6/7只有名称 |
| 3.5 推荐等级逻辑 | 确定性判定：首选复用/高性能备选/谨慎使用/不推荐，按性能阈值和放大记录区分。**删除当前"可参考"标签**，统一并入推荐等级体系，不并存两个标签 | 所有样品都是"首选复用"；"可参考"与推荐等级并存用户困惑 |
| 3.6 性能相似度独立排序 | 场景3（性能最像）按性能指标欧氏距离排序，不按配方相似度 | 性能最像和配方最像结果相同 |
| 3.7 数值精度 | 信息提取时统一精度：性能3位小数、配方4位、整数不保留小数 | 12位小数无意义 |
| 3.8 缺失字段业务化 | `missing_dimensions` 翻译为业务语言（"暂无工艺参数记录"），不出现内部术语 | "未解析动态字段"用户看不懂 |

### 层次④：回答生成

| 项 | 改动 | 解决的问题 |
|---|---|---|
| 4.1 两段式正文 | 删除"证据依据"段，统一为：结论（含推理、关键数据、推荐）+ 风险/提示 | 三段式冗余，证据依据与结论/卡片重叠 |
| 4.2 回答格式自适应 | 查询类→结论+推荐；分析类→结论+推理；方案类→结构化方案。SYSTEM PROMPT 按 answer_format 切换 | 强制三段式不适应不同场景 |
| 4.3 缺失维度明示 | 结论开头必须引用 `missing_dimensions`："EXP-128 暂无工艺参数记录，以下按原料组成匹配" | 答非所问不说明 |
| 4.4 guardrails 保留 | 因果提示、单位标注、不编造、数据不足明示，放在风险/提示段 | 手册约束 |
| 4.5 LLM不改数值 | LLM 输入已经是提取好的结构化信息，prompt 明确"数值原样引用，不得修改或估算" | LLM可能篡改数值 |

### 层次⑤：前端展示

| 项 | 改动 | 解决的问题 |
|---|---|---|
| 5.1 证据面板重构 | 默认折叠，标题"依据：结构化数据N条·文档M条"；展开后分两组：结构化数据（按样品分组KV表/diff表）、文档（标题+摘要可展开原文） | 审计表格用户看不懂、不可折叠 |
| 5.2 证据面板加载更多 | 支持分页或"加载更多"，不只显示前30条 | 257条证据只看30条 |
| 5.3 删除审计表格主展示 | EvidenceFrameCard 审计表格移到后端日志，前端不展示 | record_id/source_type/confidence 用户不需要 |
| 5.4 删除场景卡片 | 删除 RESEARCH SCENARIO 卡片（workflow名/写入策略） | 内部信息泄漏、重复出现 |
| 5.5 内部信息清理 | 主展示区只保留"数据来源：MySQL+向量库"，companyId/API限制移到后端日志 | companyId等技术细节出现在主区 |
| 5.6 技术术语业务化 | "未解析动态字段"→"部分字段未完全登记，可能有数据遗漏"；"TOPIC_ONLY"→"未建立精确关联，仅供参考" | 术语用户看不懂 |
| 5.7 warnings提升 | warnings 从折叠区提升到数据卡片下方，用业务语言 | warnings藏起来用户看不到 |
| 5.8 执行进展归并 | 14+内部节点归并为5-6用户可读阶段，内部节点下沉；单步超15s高亮 | 前置记录用户看不懂 |
| 5.9 前端三层结构 | 执行进展→引擎任务卡片→回答正文→证据面板，用分隔线区分，删除12卡片堆叠 | 卡片堆叠无分组 |
| 5.10 引擎卡片位置 | 引擎任务卡片在回答正文之前（执行过程），含图表，异步更新 | 引擎接入后图表呈现位置 |
| 5.11 引用可读化 | 回答中引用显示样品名称，不显示内部编号；点击可展开对应样品卡片 | 内部编号泄漏 |
| 5.12 省略透明化 | 数据卡片显示"共X条，展示TopN，按XX排序"；证据省略标注"按相关度省略N条，不影响结论" | 省略不透明 |
| 5.13 对齐统一 | 证据面板表格数值右对齐、文本左对齐 | 对齐方式不一致 |
| 5.14 修复用户消息重复渲染 | `App.jsx` Message 组件中用户消息的 avatar 和名称重复显示（"你 你"），删除重复渲染 | 用户消息显示"你 你" |
| 5.15 warnings统一展示去重 | warnings 只在数据卡片下方展示一次，证据面板和场景卡片中不再重复展示相同 warning；后端 `_scenario_boundary_warnings` 去重 | "部分样品存在未解析动态字段"重复出现3次 |
| 5.16 空结果/错误状态设计 | 查询无结果时显示"未找到匹配样品，建议放宽条件或检查授权范围"；LLM失败时显示友好错误+重试按钮，不显示技术栈信息 | 空结果/错误时只有技术信息 |
| 5.17 长内容滚动限制 | 数据卡片超过10个样品时显示Top10+"加载更多"；证据面板展开后最大高度400px+内部滚动；回答正文无高度限制 | 50个样品全部展开页面无限长 |

### 层次⑥：后端治理（不影响回答，但影响数据质量）

| 项 | 改动 | 解决的问题 |
|---|---|---|
| 6.1 单位一致性 | 核查 recipes 字段单位，不统一时卡片标注 | 水0.00125 vs 81.1064差4个数量级 |
| 6.2 相似度表述 | 配方相似度100%时补充"配方相同，性能可能不同"；降级匹配标注"原料组成相同，用量可能不同" | 相似度100%误导 |
| 6.3 执行详情日志化 | 前端移除的执行详情保存到后端运行日志，可通过接口查询 | 开发者仍需审计能力 |

---

## 三、已知问题覆盖性检查

| # | 已知问题 | 覆盖项 | 状态 |
|---|---|---|---|
| 1 | 只给ID不给数据 | 3.1, 3.2 | ✅ |
| 2 | 工艺相似度直接失败 | 3.2, 4.3 | ✅ |
| 3 | 性能反查只列ID | 3.4 | ✅ |
| 4 | 答非所问：问工艺答原料 | 1.2, 4.3 | ✅ |
| 5 | 研发上下文笼统说缺失 | 1.2, 3.8, 4.3 | ✅ |
| 6 | 正文三段式冗余 | 4.1 | ✅ |
| 7 | 推荐等级无区分度 | 3.5 | ✅ |
| 8 | 数值精度过高 | 3.7 | ✅ |
| 9 | 单位不一致 | 6.1 | ✅ |
| 10 | 相似度100%误导 | 6.2 | ✅ |
| 11 | 性能最像和配方最像结果相同 | 3.6 | ✅ |
| 12 | 向量检索全0 | 2.1（第一轮已完成，需验证args）, 2.2 | ✅ |
| 13 | 实体对齐错误（样品名误判项目名） | 第一轮已修 | ✅ |
| 14 | 内部信息泄漏（编号/表名/SQL） | 4.1, 5.3, 5.4, 5.5 | ✅ |
| 15 | 因果断言 | 4.4 | ✅ |
| 16 | 标题后正文丢失 | 第一轮已修 | ✅ |
| 17 | 段落堆积 | 第一轮已修 | ✅ |
| 18 | 证据卡片不可折叠 | 5.1 | ✅ |
| 19 | 证据卡片只显示30条 | 5.2 | ✅ |
| 20 | 证据卡片是审计表格 | 5.1, 5.3 | ✅ |
| 21 | 对齐方式不一致 | 5.13 | ✅ |
| 22 | 内部技术信息主展示区 | 5.4, 5.5 | ✅ |
| 23 | 技术术语未业务化 | 3.8, 5.6 | ✅ |
| 24 | 12卡片堆叠 | 5.9 | ✅ |
| 25 | 前置记录14+节点 | 5.8 | ✅ |
| 26 | 省略不透明 | 5.12 | ✅ |
| 27 | 数据卡片场景4/6/7只有名称 | 3.4 | ✅ |
| 28 | warnings藏在折叠区 | 5.7 | ✅ |
| 29 | 执行详情和进展重复 | 5.8, 6.3 | ✅ |
| 30 | EvidenceFrame扁平碎片进LLM | 3.1（LLM输入改用聚合器摘要，EvidenceFrame保留用于审计） | ✅ |
| 31 | 压缩器做内容选择 | 3.3（第一轮已完成） | ✅ |
| 32 | LLM承担信息提取 | 3.2, 4.5 | ✅ |
| 33 | 证据面板展示审计信息 | 5.1, 5.3 | ✅ |
| 34 | 强制三段式 | 4.1, 4.2 | ✅ |
| 35 | 引擎图表位置 | 5.10 | ✅ |
| 36 | 执行详情前端展示 | 6.3 | ✅ |
| 37 | 用户消息重复显示"你 你" | 5.14 | ✅ |
| 38 | "可参考"与推荐等级并存 | 3.5 | ✅ |
| 39 | warnings重复出现3次 | 5.15 | ✅ |
| 40 | 空结果/错误状态无友好提示 | 5.16 | ✅ |
| 41 | 长内容无滚动限制 | 5.17 | ✅ |

**41个已知问题全部覆盖（其中2个第一轮已完成，39个本轮整改）。**

---

## 四、用户视角审核

模拟用户提问后的体验，逐项检查：

| 用户行为 | 用户看到什么 | 是否符合预期 |
|---|---|---|
| 问"找EXP-128工艺最像的样品" | 结论开头："EXP-128暂无工艺参数记录，以下按原料组成匹配"；卡片含配方+性能 | ✅ 明确知道为什么不是工艺结果 |
| 问"密度差>100且持液量<0.1的样品" | 结论："找到50个，推荐EXP-001..."；卡片Top5含密度差/持液量/配方/推荐等级 | ✅ 有数据有推荐 |
| 看回答正文 | 两段式：结论+风险提示，无"证据依据"段 | ✅ 简洁不冗余 |
| 展开证据面板 | 结构化数据KV表 + 文档卡片，无record_id编号 | ✅ 看得懂 |
| 看执行进展 | 5-6个阶段，如"读取结构化证据→检索向量→生成报告" | ✅ 知道在做什么 |
| 找companyId/workflow名 | 找不到，已移到后端日志 | ✅ 用户不需要 |
| 看数值 | 性能3位小数，配方4位 | ✅ 不过载 |
| 看推荐等级 | 首选/备选/谨慎/不推荐，有区分；无"可参考"并存 | ✅ 能快速判断 |
| 引擎任务执行时 | 回答之前有引擎卡片含图表，回答正文是最终报告 | ✅ 过程和结果分明 |
| 数据被截断时 | 卡片显示"共50条，展示Top10，按性能余量排序"+加载更多 | ✅ 知道省略规则 |
| 自己发的消息 | 只显示一个"你"，不重复 | ✅ 正常 |
| 查询无结果 | 友好提示"未找到匹配样品，建议放宽条件" | ✅ 不困惑 |
| 长回答/长证据 | 数据卡片Top10+加载更多，证据面板400px滚动 | ✅ 页面不无限长 |
| warnings | 只在数据卡片下方出现一次 | ✅ 不重复 |

**用户视角审核通过。**

---

## 五、第一性原理审核

以"正确回答用户问题"为唯一目标，检查每个模块存在的必要性：

| 模块/功能 | 存在意义 | 是否服务于回答目标 | 决策 |
|---|---|---|---|
| 意图识别 | 知道用户问什么 | ✅ 是 | 保留 |
| 结构化查询 | 获取数据 | ✅ 是 | 保留 |
| 向量检索 | 获取文档 | ✅ 是 | 保留+优化 |
| 信息提取（聚合器） | 从数据中提取相关信息 | ✅ 是，核心 | 扶正 |
| LLM回答生成 | 组织自然语言回答 | ✅ 是 | 保留，限制不改数值 |
| 执行进展SSE | 用户知道系统在做什么 | ✅ 是 | 保留+归并 |
| 引擎任务卡片 | 展示引擎执行过程和图表 | ✅ 是（引擎场景） | 保留 |
| 证据面板 | 支撑结论的相关信息 | ✅ 是 | 重构为相关信息展示 |
| EvidenceFrame扁平碎片 | 审计、引用校验、证据面板数据源 | ✅ 是（但不进LLM主上下文） | 保留，LLM输入改用聚合器摘要 |
| 压缩器内容选择 | 决定哪些信息重要 | ❌ 否，这是信息提取的职责 | 删除，只留尺寸兜底 |
| 审计表格主展示 | 展示record_id/source_type | ❌ 否，用户看不懂 | 移到后端日志 |
| RESEARCH SCENARIO卡片 | 展示workflow名/写入策略 | ❌ 否，内部信息 | 删除 |
| 强制三段式prompt | 统一回答格式 | ❌ 否，不适应不同场景 | 改为自适应 |
| 12卡片堆叠 | 展示所有中间结果 | ❌ 否，信息过载 | 改为三层结构 |
| 执行详情前端展示 | 展示审计和技术参数 | ❌ 否，用户不需要 | 移到后端日志 |

**第一性原理审核通过：所有保留的模块都直接服务于"正确回答用户问题"，删除的都是"展示系统做了什么"的内部信息。**

---

## 六、实施顺序

```
层次③ 信息提取（核心，先做）
  3.1 LLM输入改用聚合器摘要 → 3.2 聚合器扶正 → 3.3 压缩器降级（已完成） → 3.4~3.8 提取逻辑完善
  ↓
层次④ 回答生成
  4.1 两段式 → 4.2 自适应 → 4.3 缺失明示 → 4.4~4.5 规范
  ↓
层次⑤ 前端展示
  5.1 证据面板重构 → 5.2~5.13 展示优化
  ↓
层次①② 意图和数据获取（可并行）
层次⑥ 后端治理（可并行）
```

**预估工作量**：核心层（③④）约4天，前端约2天，其余约1天，总计约7天。

---

*本方案结束。覆盖41个已知问题（2个第一轮已完成，39个本轮整改），通过用户视角和第一性原理双重审核。*
